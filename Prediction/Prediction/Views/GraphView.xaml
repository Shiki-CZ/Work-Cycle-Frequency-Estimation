<abstraction:IocControl x:Class="Prediction.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Prediction.Views"
             xmlns:abstraction="clr-namespace:Prediction.Views.Abstraction"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:viewModels="clr-namespace:Prediction.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800" d:DataContext="{d:DesignInstance viewModels:GraphViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <oxy:PlotView Grid.Row="0" Model="{Binding OxyGraph}" />
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Compute" Command="{Binding ComputeCommand}" ></Button>
        </StackPanel>
    </Grid>
</abstraction:IocControl>
